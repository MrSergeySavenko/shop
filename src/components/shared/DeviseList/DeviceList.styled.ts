import styled from '@emotion/styled';

export const SWrapper = styled('div')(() => ({
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    marginBottom: '56px',
    width: '100%',
    justifyContent: 'space-between',
}));
